-- Services
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

-- Удаление старого UI если есть
if CoreGui:FindFirstChild("NixesHub") then CoreGui.NixesHub:Destroy() end

-- UI Root
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NixesHub"
ScreenGui.Parent = CoreGui

-- Main Window (Glass Effect)
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 500, 0, 320)
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -160)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 20, 30)
MainFrame.BackgroundTransparency = 0.2
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 2
UIStroke.Color = Color3.fromRGB(0, 170, 255)
UIStroke.Transparency = 0.5
UIStroke.Parent = MainFrame

-- Left Navbar
local NavBar = Instance.new("Frame")
NavBar.Size = UDim2.new(0, 120, 1, 0)
NavBar.BackgroundColor3 = Color3.fromRGB(10, 15, 25)
NavBar.BackgroundTransparency = 0.5
NavBar.Parent = MainFrame

local NavCorner = Instance.new("UICorner")
NavCorner.CornerRadius = UDim.new(0, 12)
NavCorner.Parent = NavBar

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Text = "NIXES HUB"
Title.TextColor3 = Color3.fromRGB(0, 200, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.BackgroundTransparency = 1
Title.Parent = NavBar

-- Контейнер для страниц
local Pages = Instance.new("Frame")
Pages.Position = UDim2.new(0, 130, 0, 10)
Pages.Size = UDim2.new(1, -140, 1, -20)
Pages.BackgroundTransparency = 1
Pages.Parent = MainFrame

local function CreatePage(name)
    local p = Instance.new("ScrollingFrame")
    p.Name = name
    p.Size = UDim2.new(1, 0, 1, 0)
    p.BackgroundTransparency = 1
    p.Visible = false
    p.ScrollBarThickness = 0
    p.Parent = Pages
    
    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0, 8)
    layout.Parent = p
    return p
end

local FarmPage = CreatePage("Farm")
local EventPage = CreatePage("Event")
FarmPage.Visible = true

--- ЭЛЕМЕНТЫ УПРАВЛЕНИЯ (Пример создания кнопки) ---
local function CreateToggle(parent, text, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(30, 40, 50)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    btn.Parent = parent
    
    local state = false
    btn.MouseButton1Click:Connect(function()
        state = not state
        btn.TextColor3 = state and Color3.fromRGB(0, 255, 150) or Color3.fromRGB(200, 200, 200)
        callback(state)
    end)
    
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 6)
    c.Parent = btn
end

-- Вкладка FARM
local WeaponType = "Melee"
CreateToggle(FarmPage, "Weapon: Melee (Click to Cycle)", function(s)
    local types = {"Melee", "Sword", "Fruit"}
    local current = table.find(types, WeaponType)
    WeaponType = types[current % 3 + 1]
    FarmPage:FindFirstChild("Weapon: Melee (Click to Cycle)").Text = "Weapon: " .. WeaponType
end)

local AutoFarmEnabled = false
CreateToggle(FarmPage, "AUTO FARM LVL", function(val)
    AutoFarmEnabled = val
    if val then print("Запуск логики поиска квестера...") end
end)

-- Вкладка EVENT
CreateToggle(EventPage, "AUTO EGG HUNT", function(val)
    -- Сюда переносится твоя старая логика сбора яиц
end)

--- LOGIC: Автоматическое взятие оружия ---
task.spawn(function()
    while task.wait(1) do
        if AutoFarmEnabled then
            local char = player.Character
            if char and not char:FindFirstChildOfClass("Tool") then
                for _, tool in ipairs(player.Backpack:GetChildren()) do
                    if tool:IsA("Tool") and (tool.ToolTip == WeaponType or string.find(tool.Name, WeaponType)) then
                        char.Humanoid:EquipTool(tool)
                    end
                end
            end
        end
    end
end)

-- Переключение вкладок (Упрощенно)
local function ShowPage(name)
    for _, p in pairs(Pages:GetChildren()) do
        p.Visible = (p.Name == name)
    end
end

-- Кнопки навигации
local FarmTab = Instance.new("TextButton")
FarmTab.Size = UDim2.new(1, -10, 0, 30)
FarmTab.Position = UDim2.new(0, 5, 0, 50)
FarmTab.Text = "Farm"
FarmTab.Parent = NavBar
FarmTab.MouseButton1Click:Connect(function() ShowPage("Farm") end)

local EventTab = Instance.new("TextButton")
EventTab.Size = UDim2.new(1, -10, 0, 30)
EventTab.Position = UDim2.new(0, 5, 0, 85)
EventTab.Text = "Event"
EventTab.Parent = NavBar
EventTab.MouseButton1Click:Connect(function() ShowPage("Event") end)
